interface:
  display_name: "JIRA Auto Fix"
  short_description: "Reproduce, diagnose, fix, test, and locally commit JIRA bugs"
  default_prompt: "Use $jira-auto-fix to reproduce, diagnose, and fix JIRA-1234, then create a local commit after my approvals."
